
//#include <Servo.h>  //add '<' and '>' before and after servo.h


//Servo servoLock;
//*****************************************************************************************//
void servoSetup() {
  // Servo
//  servoLock.attach(33);

  
}

void servoLoop() {
 // openLock();
}

/*
void openLock() {
//control the servo's direction and the position of the motor

  servoLock.write(0
  );                  // rotate counterclockwise full speed
  delay(1000);
  servoLock.write(0);                  // rotate counterclockwise full speed
  delay(2000);
  servoLock.write(90);                  // rotate counterclockwise full speed
}

void tempOpenClose() {
  openLock();
  delay(10000);
  closeLock();

   
  //end control the servo's speed  
}

void closeLock() {
  servoLock.write(90);                  // stop      
  servoLock.write(90);                  // rotate counterclockwise full speed

}
*/
